import styled from "styled-components";

export const Users = () => {
  return (
    <SContainer>
      <h2>ユーザーの一覧です</h2>
    </SContainer>
  );
};

const SContainer = styled.div`
  text-align: center;
`;
